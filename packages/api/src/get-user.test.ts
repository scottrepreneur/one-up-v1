// import getUser from './get-user';

describe('getUser', () => {
  it('executes as expected', async () => {
    // const response = await getUser('test', 'test', 'test');
    expect(true);
  });
});
