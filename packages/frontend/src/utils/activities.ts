export const getPointsToday = (history: any) => {
  // const cutOffTime =
  // const timestamp = new Date();
  // return history.filter(activity => activity.)
};

export const getPointsThisWeek = (history: any) => { console.log(history); };
